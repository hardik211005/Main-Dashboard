services:
  - type: web
    name: login-app-backend
    runtime: python
    plan: free
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: FLASK_DEBUG
        value: "0"
